<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    {% block stylesheets %}
      {{ encore_entry_link_tags('app') }}
    {% endblock %}
    <title>Document</title>
  </head>
  <body class="flex justify-center">
    <img src="/assets/imgs/background.jpeg" alt="" />
    <nav class="flex logo">
      <h1>DELTASK</h1>
    </nav>
    <div class="login-wrapper">
      <form class="form" method="post">
        {% if error %}
          <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        {% endif %}
        <div class="flex flex-col sm:w-[80%] w-[90%]">
          <h2 class="h3 mb-3 font-weight-normal">LOG <span class="font-bold text-[#4d4dff]">IN</span></h2>
          {# <label for="inputEmail">Email</label> #}
          <div class="flex items-center">
            <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus placeholder="Email" />
            <i class="fa-regular fa-at"></i>
          </div>
          {# <label for="inputPassword">Password</label> #}
          <div class="flex items-center">
            <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required placeholder="Password" />
            <i class="fa-solid fa-fingerprint"></i>
          </div>
          <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}" />
        </div>

        {#
    Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
    See https://symfony.com/doc/current/security/remember_me.html

    <div class="checkbox mb-3">
        <label>
            <input type="checkbox" name="_remember_me"> Remember me
        </label>
    </div>
    #}
        <div class="flex gap-5 mt-4 cta-login justify-center items-center">
          <button class="btn btn-lg btn-primary shadow-md !px-30" type="submit">Login</button>
          <a class=" !text-[#6167FF]" href="/register">Sign up</a>
        </div>
      </form>
    </div>
  </body>
  <footer>
    <div class="flex justify-center">
      <p>© 2023 DELTASK</p>
    </div>
  </footer>
</html>
