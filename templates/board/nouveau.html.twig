{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ajouter un nouveau tableau</h1>

    {{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.lead) }}
    {{ form_row(form.enregistrer) }}
    {{ form_end(form) }}
{% endblock %}